/** Rendered on the server; no React hydration or client bundle is needed. */
export default function Brand() {
  return <img className="nav-logo-text brand-lockup" src="assets/archify-lockup-light.svg" alt="Archify" width={117} height={32} />;
}
